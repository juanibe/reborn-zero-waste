import React from "react";
import "../App.css";
import axios from 'axios';

const ManufacturerProfile = () => {
  // Will compose of the following components
  // Manufacturer Name, Brand Name
  return(
    <div className="container">
      {/* <ManufacturerDetails/> Name of Manufacturer, brand name, address, city, state, country
      <Modal /> All given details can be editable
      <ManufacturerPortfolio /> Past work, image gallery. Can be like a carousel
      <FabricList /> List Type and Quantity of Fabric
      <NewsletterHandler /> Notify me when designers post fabric requirements
       */}
    </div>
  );
}

// brand / city / email

export default ManufacturerProfile;